/* I derived the formula nCk(n, k) = PI(n-i)/k!, where i goes from 0 to k-1, PI
 * is the product, and ! is the factorial function.
 * A little help from Wikipedia yielded an even better formula, which I was
 * also able to derive, of nCk(n, k) = PI((n-i+1)/i) from i = 1 to k. */
