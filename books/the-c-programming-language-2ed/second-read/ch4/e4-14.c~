#include <stdio.h>
#define INT int
#define swap(t, x, y)\
	t temp = x;\
	x = y;\
	y = temp

int main(void) {
	int x = 0;
	int y = 9;

	printf("x: %d\ny: %d\n", x, y);
	swap(int, x, y);
	printf("x: %d\ny: %d\n", x, y);

	return 0;
}
